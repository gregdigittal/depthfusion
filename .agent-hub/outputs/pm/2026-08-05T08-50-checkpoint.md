# Checkpoint: E-73 — pm

- **Timestamp (UTC)**: 2026-08-05T08:50:02Z
- **Status**: in-progress
- **Session ID**: c3bb72dc-e582-4e11-a593-c7c146fccde6
- **Project**: depthfusion
- **Published to DepthFusion**: NO — no `mcp__depthfusion__*` tools in this session. This file is
  the only record. A resuming agent should not look for a published context item.

## What's done

- **PR #50 merged** to `main` as `1a4c946` (admin override — `main` requires 1 approving review,
  `enforce_admins: false`, and GitHub forbids self-approval by the PR author). Closed S-247,
  S-248, S-249.
- **S-248 was shipped non-functional** and is now fixed: `decay_summary.skipped` →
  `AttributeError` (real attrs are `skipped_pinned` / `skipped_already_decayed`) and `FileBus()`
  → `TypeError` (`bus_dir` required, so `hygiene_report` was never published). Both hidden by
  `MagicMock` collaborators + a broad `except Exception`. Found by **mypy**, not the 20 passing
  tests. `TestRealCollaborators` in `tests/core/test_hygiene_scheduler.py` guards both.
- **Lint cleared** — 83 ruff errors. All six Installer-CI `Test` jobs had been dying at
  `ruff check src/` before running any test.
- **Branch cleanup** — 7 local + 5 remote branches deleted, all verified stale. Rollback:
  `.agent-hub/rollback/2026-08-05T05-45-08-delete-stale-branches.sh`.
- **agent-ops S-624 filed** — commit `366a9f1f` on agent-ops `main`, **unpushed**, left there
  deliberately by user decision. DMA engine fork (see Bugs below).
- **Session brief pushed** — `fb03ff4` in `agent-hub-context`,
  `sessions/depthfusion/2026-08-05-e73-audit-merge-and-dma-dispatch.md`.
- **DMA run dispatched and approved** through the Phase E gate.

## What's in progress

**DMA run `wf_8f441b44-56c` (task `w1fnhvv7b`)** — Wave 1, actively progressing.

- Journal: 135 entries, last write 08:47:59Z →
  `~/.claude-acc2/projects/-home-gregmorris-projects-depthfusion/c3bb72dc-e582-4e11-a593-c7c146fccde6/subagents/workflows/wf_8f441b44-56c/journal.jsonl`
- Branch `feat/e73-s250-s254` checked out in the **main working tree** (not a worktree).
- **HEAD is still `1a4c946` — no wave commit yet.** All work is uncommitted:
  `14 files changed, 1203 insertions(+), 43 deletions(-)` plus 8 untracked new files:
  `scripts/mcp-smoke-test.sh`, `src/depthfusion/mcp/session_activity.py`,
  `tests/core/test_event_store.py`, `tests/mcp/test_ambient_trace_wiring.py`,
  `tests/mcp/test_http_server.py`, `tests/mcp/test_session_checkpoint.py`,
  `tests/mcp/test_session_seed_resume.py`, `tests/test_retrieval/test_ambient_trace_filter.py`
- Config: worker `kimi-k3` (moonshot), verifier `grok-4-5` (xai), tiebreaker dynamic,
  budget `unlimited` ($200 per-run ceiling). Est. $0 metered / 6.5–14% subscription window.
- Suite at last check: **3766 passed**, 38 skipped (baseline was 3708).
- Story state: **S-246 complete** (3/3 AC, 3/3 tasks). **S-250** AC-1/2/3/5 + T-847/848/849/851/852
  ticked; AC-4 + T-850 marked deferred (but see Bugs — they are actually implemented).

**Three watches armed** (all die with the session; re-arm on resume):

| Task | Fires when |
|---|---|
| `bam23z763` | first commit lands on `feat/e73-s250-s254` |
| `bsbyzon46` | workflow journal idle >1h with no result |
| `w1fnhvv7b` | the run completes or escalates |

## What remains

- [ ] Wave 1 merge gate → first commits on `feat/e73-s250-s254`
- [ ] Waves 2–4: S-251 (rate limiting), S-253 (CheckpointTimeline), S-252 (SSE stream),
      S-254 (FileDiffHistory), then final gate + PR
- [ ] **Raise the T-850/AC-4 discrepancy at the final gate** (see Bugs) — do not edit BACKLOG.md
      while the run owns it
- [ ] Expect the run to terminate at a **blocked** PR (required-review rule, same as PR #50)
- [ ] Triage/re-home agent-ops S-624 to the owning DMA epic; decide whether to push `366a9f1f`
- [ ] 36 Dependabot vulns on DepthFusion `main` (21 high, 12 moderate, 3 low) — unaddressed,
      squarely in E-73's remit

## Open decisions

- **Push agent-ops `366a9f1f`?** Committed to agent-ops `main` (which already carried 4 unpushed
  commits from other sessions). User chose "leave it". Decider: user.
- **Registry staleness accepted, not refreshed** (`generatedAt: 2026-07-25`, weekly cadence).
  Refresh needs `model-research.weekly.workflow.js`, which is a workflow — user has asked that
  workflows not be run unprompted. Decider: user.
- **Grok is verifier, not tiebreaker.** `tiebreakerModelOverride` is documented but unimplemented
  in the deployed engine, so Grok was swapped into the verifier slot instead. This moves Gate-5
  grading off the Codex family, against the Fable-5 default. Worth reviewing whether Grok's
  grading proved as strict. Decider: user.

## Bugs/issues surfaced during this work

- **DMA engine fork** — Critical to the tooling, not to this repo. Two copies, neither a superset:
  `~/.claude/workflows/digittal-method-auto.workflow.js` (170,806 B — what the skill dispatches;
  `TRINITY_DEFAULT`=0, `tiebreakerModelOverride`=0, halt guards=3) vs
  `agent-ops/tools/dma/engine/digittal-method-auto.workflow.js` (406,829 B; 6, 6, 2). Filed as
  agent-ops S-624. Blocking current task? No.
- **T-850/AC-4 recorded as deferred but actually implemented** — Medium. BACKLOG.md says "not
  started; deferred", yet `depthfusion_session_checkpoint` is registered at `_registry.py:213,268,741`,
  handler at `mcp/tools/project.py:339`, dispatched at `mcp/server.py:82,148`. Inverse of the S-248
  failure — shipped but recorded absent. Likely registered incidentally during T-849 and therefore
  **unverified**. Blocking? Not now; blocks S-250 closure.
- **Test suite was touching the production graph store** — found by the run itself.
  `_get_fabric_store()`'s lazy singleton defaulted to `~/.claude/depthfusion-graph.{json,db}`.
  Now guarded in `tests/conftest.py`. Blocking? No — fixed.
- **`git branch -r` reads stale local refs** — a plain `git fetch` never prunes, so five
  long-deleted branches appeared live. Use `--prune` or `gh api .../branches`. Blocking? No.

## Resume hint

1. **Check the run first — do not assume it died with the session.** Read the last 5 entries of
   `journal.jsonl` (path above) and compare its mtime to now. If it is still advancing, the
   workflow survived; just re-arm the three watches and wait.
2. **If the run is dead**, inspect `git status` on `feat/e73-s250-s254`. Wave 1's ~1200 lines are
   **uncommitted in the main working tree** — they survive a session death but not a
   `git checkout .` or `git reset --hard`. Commit them to a safety branch before doing anything
   else.
3. **To resume the engine**: `Workflow({scriptPath: "/home/gregmorris/.claude/workflows/digittal-method-auto.workflow.js",
   resumeFromRunId: "wf_8f441b44-56c", args: {...same args, approvedBudget: "unlimited",
   workerModelOverride: "kimi-k3", verifierModelOverride: "grok-4-5"}})`. Use `scriptPath`, never
   `name:` (caches a stale script within a session). If the run returned `GATE_UNVERIFIABLE` or
   `USER_ESCALATION_POST_RECURSION`, **do not resume** — that replays the cached failure; start a
   fresh run scoped to the remaining tasks.
4. **Never implement directly** on `ZERO_DISPATCH_RUN`, `PHASE_B_PROJECT_UNREADABLE`, or
   `INTERVIEW_PAUSED_FOR_CLARIFICATION` — surface to the user and stop.
5. Plan of record: `~/.claude-acc2/plans/proud-wandering-forest.md`.

## Standing quality rule for this epic

Audit every AC against its **literal text**, never against the test suite — tests can agree with
the implementation while both diverge from the spec. Any story touching an external collaborator
(Redis, event bus, SSE queue, git) needs one test built on the **real** object, proven to fail by
reintroducing the bug. mypy is a hard gate. A red lint can hide a red test.
