# V2-DEC-001 — DRY-RUN-G0-SPLIT-2: DoS surface in expression_eval.py

**Decision ID:** V2-DEC-001  
**Ticket:** DRY-RUN-G0-SPLIT-2  
**Status:** RESOLVED  
**Date:** 2026-06-10  
**Resolved:** 2026-06-11  
**Resolution commit:** _(see below)_  
**Tiebreak advisory lean:** reviewers (DoS findings are real; do not defer)

> **Lane halt lifted.** All four DoS guards (F2/F3/F5/F6) implemented and
> passing in `test_expression_eval.py`. `tests/test_dry_run_split.py` removed.
> `expression_eval.py` is no longer blocked for production endpoints.

---

## What split

The consensus pipeline ran `src/depthfusion/utils/expression_eval.py` through the
security-critical work class (dev=opus, reviewers=deepseek+gemini). After the
rebuttal round, both reviewers maintained their objections on five findings:

| ID | File | Severity | Claim |
|----|------|----------|-------|
| F1 | scripts/v2/review-deepseek.sh:11 | HIGH | `set -euo pipefail` + unguarded `git diff` fallback aborts on invalid ref |
| F2 | src/depthfusion/utils/expression_eval.py:117 | MEDIUM | No length guard before `ast.parse` — O(n) work on arbitrarily large input |
| F3 | src/depthfusion/utils/expression_eval.py:123 | MEDIUM | Unbounded recursion in `_eval_node` — `RecursionError` escapes `ExpressionError` contract |
| F5 | src/depthfusion/utils/expression_eval.py:49 | MEDIUM | `ast.LShift` has no magnitude guard — `1 << 1_000_000` allocates a 1M-bit integer |
| F6 | src/depthfusion/utils/expression_eval.py:41 | MEDIUM | `ast.Mult` has no type check — `[0] * 100_000_000` allocates a 100M-element list |

Full adjudication: see `docs/decisions/rebuttal-DRY-RUN-G0-SPLIT-2.md`.

## Tiebreak advisory

Called via `mcp__depthfusion__depthfusion_bridge` with model `openai/gpt-4o`.

**Lean: reviewers.**

> Both sides agree on the facts: all findings are accepted by DEV and confirmed
> present in the code. The dispute is procedural — DEV deferred all fixes rather
> than implementing them, and reviewers are correctly refusing to sign off on
> unimplemented remediations.
>
> The two HIGH findings (F1: `set -euo pipefail` + missing `|| true` in both
> reviewer scripts) reproduce on the exact SHA and break the review pipeline
> tooling. The four MEDIUM findings in `expression_eval.py` (F2: no length guard;
> F3: bare `RecursionError` escaping the `ExpressionError` contract; F5: LShift
> no magnitude guard; F6: Mult over sequence types unbounded) are all present and
> were empirically reproduced by DEV.
>
> DEV's honest repudiation of the false trust-based brief is correct and should be
> upheld. But repudiation alone does not fix the code. Reviewers are correct to
> object until the guards are in.

*(Advisory from `v2-consensus-ticket.js` forced-split dry-run, workflow we5cye6le)*

## Resolution

The implementing round (Phase 1) must address F1–F3, F5–F6 before `expression_eval.py`
ships to any production endpoint. F4 (BoolOp pre-evaluation) is narrowed to a
cosmetic/documentation issue and is deferred to a follow-up story.

**Lane halt:** LIFTED. `resolved: true` — tickets touching `expression_eval.py`
may proceed through the consensus pipeline without the halt check triggering.

---

*Generated by the V2 consensus pipeline (v2-consensus-ticket.js). See
`docs/decisions/rebuttal-DRY-RUN-G0-SPLIT-2.md` for the full per-finding record.*
