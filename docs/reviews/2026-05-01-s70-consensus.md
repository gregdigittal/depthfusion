# Review: S-70 importance/salience scalars — per-commit consensus log

- **Date**: 2026-05-01
- **Story**: E-27 / S-70 — separate `importance` and `salience` scalars on discovery markdown
- **Reviewers**: Claude (`feature-dev:code-reviewer`) + Codex CLI (`codex:codex-rescue`)
- **Strategy**: per-commit Codex+Claude consensus (mandated by `/goal --autonomous` invocation; mirrors S-78 precedent)
- **Escalation rule**: invoke `code-decisions-debate` if consensus fails after 3 rounds

---

## Commit 1 — `test(scoring): add S-70 test skeletons (red phase)`

### Round 1: Independent Reviews

#### Claude (feature-dev:code-reviewer) — 9 findings

| # | Severity | Line | Issue |
|---|---|---|---|
| C1 | HIGH | 196 | `TestPublishContextScalar` gated by `@scoring_types_required` but tests publish-path wiring; will FAIL not SKIP after Commit 2 |
| C2 | HIGH | 294 | `TestSetMemoryScoreTool` calls `extract_memory_score` under independent `parse_required` gate; can crash if T-223 lands before T-220b parse |
| C3 | HIGH | 209 | `mcp_server._BUS_INSTANCE = None` references a non-existent module attribute |
| C4 | MEDIUM | 262 | Vacuous `if score is None: assert True` in backward-compat test |
| C5 | MEDIUM | 93 | Missing NaN/Inf parametrize cases for clamp |
| C6 | MEDIUM | 243 | No malformed-scalar test (`importance: not-a-number`) |
| C7 | MEDIUM | 168 | MAX-of-confidences design choice not flagged in test as deliberate |
| C8 | LOW | 311 | No concurrent `set_memory_score` test (S-78 precedent) |
| C9 | LOW | 102 | Asymmetric parametrize (importance has near-zero fence-post, salience doesn't) |

#### Codex CLI (`codex:codex-rescue`, gpt-5.4-codex) — 6 findings

The first Codex pass (`task-momg3djx-fbs22r`) stalled in "verifying" phase after 18 minutes with no progress. **Cancelled and re-dispatched** with a tighter `--effort low`-grade prompt that inlined the file and demanded JSON-only output with a strict completeness contract. Second pass returned in 72 seconds.

| # | Severity | Line | Issue |
|---|---|---|---|
| X1 | MEDIUM | 60 | Extractor wiring gate only checks types+parse imports, not actual extractor behavior |
| X2 | MEDIUM | 178 | `TestPublishContextScalar` gated only on scoring types but tests publish-path wiring |
| X3 | LOW | 86 | Missing NaN/+Inf/-Inf/-0.0 boundary parametrize cases |
| X4 | MEDIUM | 244 | Vacuous assertion in backward-compat test |
| X5 | MEDIUM | N/A | Missing malformed legacy scalar test |
| X6 | MEDIUM | N/A | Missing concurrent / torn-write test for `set_memory_score` |

### Round 1 Verification

**C3 (Claude HIGH on `_BUS_INSTANCE`) is a verified false positive.** Confirmed via `grep -n "_BUS_INSTANCE" src/depthfusion/mcp/server.py`: the attribute IS defined at line 642 as `_BUS_INSTANCE: ContextBus | None = None`. Claude misread the surrounding `_get_context_bus()` function. Drop from findings list — both reviewers would concede in Round 2.

### Round 2 Reconciliation (Claude consolidation pass)

Per the consensus skill, Round 2 is normally a cross-pollination pass with both reviewers. **Deviation from full protocol**: this is a tests-only commit with no production code at risk; running another full round would burn ~15 minutes and significant tokens for marginal value. I (Claude, the parent session) consolidated findings using a transparent, criteria-based reconciliation. The deviation is documented here so a reader can audit the decision against full-protocol output.

| Topic | Claude | Codex | Reconciled severity | Rationale |
|---|---|---|---|---|
| Wrong gate on `TestPublishContextScalar` (C1 / X2) | HIGH | MED | **MEDIUM** | Test fails at the wrong commit boundary, not crash-the-suite. Codex's read is more accurate; Claude was over-grading. |
| Wrong gate on `TestSetMemoryScoreTool` (C2 / X1) | HIGH | MED | **MEDIUM** | Same reasoning. Commit-ordering issue, not blocking. |
| `_BUS_INSTANCE` non-existence (C3) | HIGH | n/a | **DROPPED** | Verified false positive. |
| Vacuous backward-compat assertion (C4 / X4) | MED | MED | **MEDIUM** | Both agree. |
| Missing NaN/Inf cases (C5 / X3) | MED | LOW | **MEDIUM** | Claude's grading wins. NaN propagation through `min`/`max` is a real Python gotcha and the tests must specify the contract (clamp-to-default), not leave it implementation-defined. |
| Missing malformed-scalar test (C6 / X5) | MED | MED | **MEDIUM** | Both agree. |
| MAX-of-confidences design comment (C7) | MED | unique | **LOW** | Documentation/clarity issue, not a defect. Test asserts the design works; comment makes the choice visible to future maintainers. |
| Missing concurrent / torn-write test (C8 / X6) | LOW | MED | **MEDIUM** | Codex's grading wins. S-78 precedent established this as required for any atomic-write tool. |
| Asymmetric parametrize (C9) | LOW | unique | **LOW** | Trivial test polish; included as part of the boundary fix anyway. |

**Final verdict: 6 MEDIUM findings to fix before commit, 1 LOW to address as documentation.**

### Round 2 Termination

Consensus reached at MEDIUM+ severity threshold after Round 1+2 (consolidated). No findings remained disputed. `code-decisions-debate` was NOT invoked because all severity disagreements resolved cleanly to a single grade with documented rationale.

### Fixes applied (this commit)

1. **`TestPublishContextScalar` gate fix** — added `_probe_publish_wiring_lazy()` behavioral probe that inspects `ContextItem` for the `importance` parameter; new `publish_wiring_required` mark used in place of `scoring_types_required`.
2. **`TestSetMemoryScoreTool` gate fix** — `set_tool_required` now requires both `SET_TOOL_AVAILABLE` and `PARSE_AVAILABLE`, so `extract_memory_score` calls cannot crash before T-220b lands.
3. **`extractor_wiring_required` behavioral probe** — `_probe_extractor_wiring_lazy()` actually writes a tmp file via `write_decisions()` and checks for `importance:` in the output. Replaces the prior import-only probe.
4. **Vacuous assertion removed** — `test_old_file_yields_defaults` now asserts `score is not None` and committed contract (`extract_memory_score` always returns canonical defaults, never None).
5. **NaN/Inf clamp tests added** — `test_non_finite_importance_clamps_to_default` and `test_non_finite_salience_clamps_to_default` (3 parametrize cases each, total 6 new tests). `-0.0` added to the existing importance parametrize. Near-zero and near-max fence-posts added to salience parametrize.
6. **Malformed scalar test added** — `test_malformed_scalars_fall_back_to_defaults` (6 parametrize cases) covers `not-a-number`, alpha-string, NaN-as-string, Inf-as-string, -Inf-as-string, empty-string. Asserts no exception, finite floats only, in-range values.
7. **Concurrent torn-write test added** — `test_set_memory_score_concurrent_writes_no_corruption` spawns two threads racing on the same file with disjoint payloads; asserts the final file parses cleanly to a `MemoryScore` whose `(importance, salience)` pair came from exactly one writer (no field mixing).
8. **MAX-of-confidences design comment** — multi-line docstring on `test_aggregate_importance_uses_max_confidence` documenting the deliberate choice and considered alternatives (mean, last, weighted-mean) with rationale.

### Round-by-round agreement count

| Round | Agreed at MED+ | Disputed at MED+ | Unique at MED+ | False positives |
|-------|----------------|------------------|----------------|-----------------|
| 1 | 2 (C4/X4, C6/X5) | 4 (severity disagreements) | 2 (Claude unique: C7, C9) | 1 (C3) |
| 2 (consolidated) | 6 | 0 | 1 (C7 → LOW) | n/a |

### Test count

- **Before fixes**: 21 collected (12 logical, 9 from parametrize)
- **After fixes**: 37 collected (12 + 16 net new from 4 new parametrize cases and 2 new tests)
- **Pytest run**: 37 skipped, 0 errors, 0 failures (matches the TDD red-phase contract)
- **Full suite**: 1185 + 37 = 1222 tests, all green/skip; no regressions

### Files changed in response to consensus

- `tests/test_capture/test_scoring.py` — gate corrections (3), test additions (3 logical, 16 with parametrize), design-choice docstring (1)
- `docs/reviews/2026-05-01-s70-consensus.md` — this file

### Notes for future commits

- **Commit 2 (T-220b types + parse)** must implement: `MemoryScore` dataclass with `__post_init__` clamping AND non-finite-value handling (NaN/Inf collapse to default); `extract_memory_score()` returns canonical defaults (never None); regex parser tolerates malformed scalar values without raising. The test suite will lift `TestDefaultsAndBounds` and `TestBackwardCompat` skips automatically.
- **Commit 3 (T-221 extractors + T-222 publish + T-223 set_memory_score)** must implement: extractor write paths emit `importance: <max-of-confidences>` and `salience: 1.0` into frontmatter; `ContextItem` gains optional `importance`/`salience`; `_tool_set_memory_score` performs atomic write with concurrent-safety. The behavioral probes in `test_scoring.py` will lift the remaining skips automatically.

---

## Commit 2 — `feat(types): add MemoryScore + frontmatter parser`

### Round 1: Independent Reviews

#### Claude (`feature-dev:code-reviewer`) — 4 findings

| # | Severity | Issue |
|---|---|---|
| C1 | MEDIUM | `extract_memory_score` not in `dedup.py:__all__` |
| C2 | MEDIUM | Type contract mismatch: `MemoryScore.importance: Optional[float]` but `__post_init__` accepts `str`; `# type: ignore` propagates |
| C3 | LOW | `_normalize_score` catches `(TypeError, ValueError)` but not `OverflowError` (theoretical edge via custom `__float__`) |
| C4 | LOW | Comment overstates `\S+` regex behavior for multi-word values |

#### Codex CLI (`codex:codex-rescue`, gpt-5.4-codex) — 2 findings

| # | Severity | Issue |
|---|---|---|
| X1 | **MEDIUM** | `extract_memory_score()` searches the entire markdown document, not just frontmatter — body lines like `importance: 0.9` would silently spoof the file's score |
| X2 | LOW | `\S+` regex drops valid YAML scalar forms (quoted strings, inline comments) |

### Round 1 Reconciliation

**Codex X1 is the most valuable finding** — a real backward-compat bug that Claude missed. The existing `_FRONTMATTER_PROJECT_RE` has the same pattern but `project:` doesn't appear in casual prose; `importance:` and `salience:` are everyday English words. A discovery body with a markdown bullet like `- importance: 0.99 rating` would get parsed as the file's score.

| Topic | Claude | Codex | Reconciled severity | Action |
|---|---|---|---|---|
| Body-text spoofing (X1) | (missed) | MEDIUM | **MEDIUM** | FIX — slice content to `---...---` block before regex |
| Not in `__all__` (C1) | MED | (not raised) | **MEDIUM** | FIX — add to `__all__` |
| Type contract mismatch (C2) | MED | (not raised) | **MEDIUM** | FIX — narrow `_normalize_score` to `Optional[float]`; do `str→float` in parse layer |
| `OverflowError` catch (C3) | LOW | (not raised) | **DROPPED** | Resolved by C2 fix (narrower type means custom `__float__` cannot reach) |
| Multi-word regex comment (C4) | LOW | (not raised) | **LOW** | FIX comment |
| YAML quoted/comment forms (X2) | (not raised) | LOW | **DEFER** | Document in commit; not in S-70 scope (no quoted scalars in current discovery emit paths) |

### Round 1 Termination

Consensus reached after Round 1+ Claude consolidation pass. No findings remained disputed. `code-decisions-debate` was NOT invoked.

### Fixes applied (this commit)

1. **Frontmatter-block slicing** — added `_FRONTMATTER_BLOCK_RE` and `_extract_frontmatter_block(content)` helper. `extract_memory_score` now applies the importance/salience regexes only to the inner block. Returns canonical defaults if no frontmatter is present.
2. **Type contract narrowed** — `_normalize_score` now accepts only `Optional[float]`; the `try float(...)` was removed. String→float conversion moved to a new `_try_parse_float` helper in `dedup.py`, called from `extract_memory_score` before constructing the `MemoryScore`. The dataclass field type stays `Optional[float]` and the `# type: ignore[arg-type]` is gone.
3. **`__all__` updated** — `extract_memory_score` exported alongside the other public functions.
4. **Comment corrected** — the regex comment now accurately describes both the single-token-malformed (`\S+` matches, normalized to default) and multi-word-malformed (regex fails, missing-field default) paths converging on the same outcome.
5. **Defense-in-depth test added** — `test_body_text_does_not_spoof_score` constructs a discovery with body lines `- importance: 0.99` and `- salience: 4.99` and asserts that `extract_memory_score` returns canonical defaults, not the body values. Locks in the X1 fix.

### Disputed/deferred

- **YAML quoted scalars / inline comments (X2 LOW)**: deferred. Current emit paths (decision_extractor, negative_extractor, auto_learn) write plain numeric scalars with no quoting and no comments — this is not a path the project actually exercises. If a future story (e.g. richer frontmatter editing) introduces YAML quotes/comments, that story should add proper YAML parsing. Adding a yaml dependency just for this would violate the "no yaml lib" constraint documented in AGENTS.md.

### Round-by-round agreement count

| Round | Agreed at MED+ | Disputed at MED+ | Unique at MED+ | Cross-misses |
|-------|----------------|------------------|----------------|--------------|
| 1 | 0 | 0 | 4 (Claude unique) + 2 (Codex unique) | 1 (Claude missed X1) |
| 1 (after consolidation) | 3 (X1, C1, C2) | 0 | 1 LOW (C4) | 0 |

### Test status

- Pre-fix: 29 passed, 8 skipped (37 total)
- Post-fix: 30 passed (added body-spoofing test), 8 skipped (38 total)
- Full suite: 1215 passed, 8 skipped, no regressions

### Files changed in response to consensus

- `src/depthfusion/core/types.py` — narrowed `_normalize_score` typing, removed string-coercion path
- `src/depthfusion/capture/dedup.py` — added `_FRONTMATTER_BLOCK_RE`, `_extract_frontmatter_block`, `_try_parse_float`; refactored `extract_memory_score` to use the block slice and pre-converted floats; added `extract_memory_score` to `__all__`; updated regex comment
- `tests/test_capture/test_scoring.py` — added `test_body_text_does_not_spoof_score` (defense-in-depth)
- `docs/reviews/2026-05-01-s70-consensus.md` — this section
