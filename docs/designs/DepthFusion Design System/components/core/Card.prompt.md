**Card** — surface container with an optional title row.

```jsx
<Card title="Server">
  <label className="df-field-label">Server URL</label>
  …
</Card>
```

- `title` renders a heading at the top. Use for settings sections, panels, grouped content. For dashboard metric tiles use the `df-tile` markup pattern instead.
