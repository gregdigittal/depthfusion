**Avatar** — circular initial avatar (amber fill, ember glow).

```jsx
<Avatar name="Greg Morris" />
<Avatar name="Greg Morris" size={32} />
```

- Shows the first letter of `name`. `size` sets the diameter (font scales with it).
