**NodeChip** — knowledge-graph node-type chip.

```jsx
<NodeChip type="doc" />
<NodeChip type="concept">Concept</NodeChip>
<NodeChip type="decision" />
```

- `type`: `doc` (amber), `concept` (amber-soft), `decision` (warm). Pass children to include an icon or custom label.
